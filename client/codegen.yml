overwrite: true
schema: '../server/api.graphql'
documents: 'src/**/*.gql'
generates:
    generated/graphql.tsx:
        plugins:
            - 'typescript'
            - 'typescript-operations'
            - 'typescript-react-apollo'
        config:
            withHooks: true
